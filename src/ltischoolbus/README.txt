Modify the ltibridge.cnf.example file to taste, and place it into
$HOME/.ssh/ltibridge.cnf. The $HOME directory must be the home of the
user who starts the bridge. Ensure that .ssh is only read/writable by
owner. More details in <proj_root>/README.md, and in class header.

Start server via src/ltischoolbus/lti_schoolbus_bridge

